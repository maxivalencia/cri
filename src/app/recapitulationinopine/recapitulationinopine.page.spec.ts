import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RecapitulationinopinePage } from './recapitulationinopine.page';

describe('RecapitulationinopinePage', () => {
  let component: RecapitulationinopinePage;
  let fixture: ComponentFixture<RecapitulationinopinePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RecapitulationinopinePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
